enum Movement { left, right, forward, backward, up, down }

enum ParallelPlane { xy, yz, xz }

enum FaceDirection { north, south, east, west, up, down }

// enum HeadDirection { north, south, east, west, up, down }